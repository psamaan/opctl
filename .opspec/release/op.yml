description: runs all ops necessary to perform a release
name: release
inputs:
- { string: , name: CODECOV_TOKEN, description: 'A https://codecov.io/ auth token', isSecure: true }
- { string: , name: GIT_BRANCH, description: 'The git branch the coverage should be associated with' }
run:
  serial:
  - op: build
  - op: publish-coverage-to-codecov
