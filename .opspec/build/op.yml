description: builds binaries for all supported platforms/architectures
name: build
run:
  serial:
  - op: test
  - parallel:
    - op: build-bin-darwin
    - op: build-bin-linux
    - op: build-bin-windows
